import styled from 'styled-components';

export const LobbyMainContent = styled.div`
    display: grid;
    grid-template-columns: 520px 1fr;
    gap: 32px;
`;

export const Panel = styled.div`
    display: flex;
    flex-direction: column;
    gap: 32px;
`;

export const ButtonsWrapper = styled.div`
    display: flex;
    flex-direction: column;
    gap: 16px;
`;
